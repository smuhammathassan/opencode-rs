Agent 20 — runtime evidence (packaging / installation / updates / ops)
Captured 2026-08-05. Binary: /root/opencode-rs/target/release/opencode (8,054,560 bytes, ELF x86-64, not stripped)

== --version ==
$ opencode --version
1.18.13            (exit 0)   # matches reference /root/.opencode/bin/opencode --version = 1.18.13

== upgrade (stub) ==
$ opencode upgrade 1.18.13   -> "Using method: unknown" ... "opencode upgrade skipped: 1.18.13 is already installed" (exit 0)
$ opencode upgrade 99.0.0    -> "Using method: unknown" ... "From 1.18.13 -> 99.0.0" ... "the Rust port is installed in-process; automatic upgrades are not supported." (exit 0)
  # No install performed; --method value ignored; never writes any file.

== uninstall (real for data dirs) ==
$ opencode uninstall --help  -> -c/--keep-config -d/--keep-data --dry-run -f/--force (matches ref flag set)
  # removes data/cache/config/state dirs; does NOT remove binary. dry_run/force logic implemented in source.

== run (headless, BROKEN) ==
$ opencode run hello
Error: the in-process opencode server is not wired yet in this build (TODO(integration): oc-server)
Try `opencode run --attach <url>` to connect to a running opencode server.   (exit 1)
  # differential artifacts/03-cli/run.json: 'run hello' ref exit 0, rust exit 1 (also --format json, hello world, -- hello)

== default TUI (BROKEN) ==
$ opencode   -> "opencode: starting TUI (requires a TTY)" (exit 0)   [in non-TTY]
  # source: even in a TTY returns Err("the TUI is not yet wired in this build (TODO(integration): oc-tui)")

== serve (bare socket, no HTTP API) ==
$ opencode serve --port 4097 -> "opencode server listening on http://127.0.0.1:4097"
  # source serve.rs:40-62 binds a raw TCP listener that reads+discards bytes; no HTTP routes served.
  # oc-server crate is not wired into the CLI.

== stubs (64 "not yet wired" in oc-cli) ==
  session list, stats, export, completion bash, agent list, mcp add, plug/plugin install,
  debug config/rg/lsp/file/scrap/skill/snapshot/agent, generate, web interface, attach, mini, db query, console

== logging / diagnostics (ABSENT) ==
$ opencode debug info --print-logs --log-level DEBUG  -> zero log output on stderr
  - opencode.log at ~/.local/share/opencode/log/opencode.log (9,555,008 B, written by the REFERENCE binary)
  - size unchanged before/after running the Rust binary  -> port writes nothing, no tracing subscriber

== network endpoints referenced in crates/ ==
  https://api.github.com/repos/sst/opencode/releases/latest   (upgrade.rs:43)  [reference uses anomalyco/opencode]
  https://models.opencode.ai                                  (models fetch)
  https://registry.npmjs.org/                                 (plugin install, unwired)
  github.com/BurntSushi/ripgrep releases 15.1.0               (rg binary auto-download, on demand)
  opencode.ai / app.opencode.ai / opncd.ai  (strings only; not runtime calls)
  NO posthog / sentry / telemetry / analytics anywhere in crates/

== environment ==
  system tools present: rg git tar unzip curl bash zsh
  opencode.debug paths: data ~/.local/share/opencode, bin ~/.cache/opencode/bin, log, cache, config, state, tmp
