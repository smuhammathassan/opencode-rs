//! Project types.
//! From reference/packages/schema/src/project.ts.
//!
//! Canonical home: `oc_schema::project`.

// Re-export shim: `oc_schema::project` is the single canonical definition.
pub use oc_schema::project::{Info as ProjectInfo, Time as ProjectTime};
